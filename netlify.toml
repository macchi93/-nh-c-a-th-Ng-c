[build]
  command = "npm run build"
  publish = "dist"

# Cấu hình plugin html-validate trỏ đúng thư mục publish là "dist" thay vì "public"
[[plugins]]
  package = "netlify-plugin-html-validate"
  [plugins.inputs]
    validatorDir = "dist"
